<Page x:Class="WpfApp1.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Page.Resources>
        <!-- Animated gradient background brush (we'll animate GradientStop colors from code-behind) -->
        <LinearGradientBrush x:Key="AnimatedBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop x:Name="GS1" Color="#FF0B486B" Offset="0" />
            <GradientStop x:Name="GS2" Color="#FF3B8D99" Offset="1" />
        </LinearGradientBrush>


        <!-- Visual that will be used as texture on a 3D rectangle: a TextBlock centered with effects -->
        <DataTemplate x:Key="TextVisualTemplate">
            <Grid Width="600" Height="200">
                <Border CornerRadius="12" Background="#00FFFFFF">
                    <TextBlock Text="Hello"
FontSize="120"
FontWeight="Bold"
HorizontalAlignment="Center"
VerticalAlignment="Center"
TextAlignment="Center"
FontFamily="Segoe UI"
RenderTransformOrigin="0.5,0.5">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#66000000" BlurRadius="12" ShadowDepth="6" />
                        </TextBlock.Effect>
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFD54F" Offset="0"/>
                                <GradientStop Color="#FFFF7043" Offset="1"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                    </TextBlock>
                </Border>
            </Grid>
        </DataTemplate>
    </Page.Resources>


    <Grid Background="{StaticResource AnimatedBackground}">
        <!-- 3D Viewport -->
        <Viewport3D Name="MainViewport" ClipToBounds="True">
            <!-- Camera -->
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="Camera" Position="0,0,3.5" LookDirection="0,0,-3.5" UpDirection="0,1,0" FieldOfView="45" />
            </Viewport3D.Camera>


            <!-- Lights -->
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <DirectionalLight Color="#FFFFFF" Direction="-0.5,-0.5,-1" />
                </ModelVisual3D.Content>
            </ModelVisual3D>


            <!-- 3D rectangle with the text visual as a brush -->
            <ModelVisual3D x:Name="TextModelVisual">
                <ModelVisual3D.Content>
                    <GeometryModel3D x:Name="TextGeometryModel">
                        <GeometryModel3D.Geometry>
                            <MeshGeometry3D Positions="-1.5,0.5,0 1.5,0.5,0 1.5,-0.5,0 -1.5,-0.5,0"
TextureCoordinates="0,0 1,0 1,1 0,1"
TriangleIndices="0 1 2 0 2 3" />
                        </GeometryModel3D.Geometry>
                        <GeometryModel3D.Material>
                            <DiffuseMaterial>
                                <DiffuseMaterial.Brush>
                                    <!-- VisualBrush pointing at a control we create using the DataTemplate -->
                                    <VisualBrush x:Name="TextVisualBrush" Stretch="Fill">
                                        <VisualBrush.Visual>
                                            <ContentControl ContentTemplate="{StaticResource TextVisualTemplate}" />
                                        </VisualBrush.Visual>
                                    </VisualBrush>
                                </DiffuseMaterial.Brush>
                            </DiffuseMaterial>
                        </GeometryModel3D.Material>
                        <GeometryModel3D.Transform>
                            <Transform3DGroup>
                                <RotateTransform3D x:Name="RotateTransform3D">
                                    <RotateTransform3D.Rotation>
                                        <AxisAngleRotation3D x:Name="Rotation" Axis="0,1,0" Angle="0" />
                                    </RotateTransform3D.Rotation>
                                </RotateTransform3D>
                                <TranslateTransform3D OffsetX="0" OffsetY="0" OffsetZ="0" />
                            </Transform3DGroup>
                        </GeometryModel3D.Transform>
                    </GeometryModel3D>
                </ModelVisual3D.Content>
            </ModelVisual3D>
        </Viewport3D>


        <!-- Subtle overlay UI for instructions / controls (optional) -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12" Orientation="Horizontal" Opacity="0.7">
            <TextBlock x:Name="tbWelcomeMessage" Text="Hi !!!" Foreground="White" FontSize="50"/>
        </StackPanel>
    </Grid>
</Page>
