<Window x:Class="ToTheLicense.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="לרישיון" Height="480" Width="360"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- צבעי נהיגה -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FFDDA800"/>
        <!-- צהוב -->
        <SolidColorBrush x:Key="SecondaryBrush" Color="#222222"/>
        <!-- שחור -->

        <!-- כפתור סגור -->
        <Style x:Key="CloseButtonTheme" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Red"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- כפתור מודרני -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFFFCC33"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFCC9900"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White" Margin="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="15" Opacity="0.3" Color="#444"/>
        </Border.Effect>

        <Grid>
            <!-- כפתור סגור -->
            <Button x:Name="CloseButton" Content="X" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0" Style="{StaticResource CloseButtonTheme}" Click="CloseButton_Click" Foreground="Red"/>

            <StackPanel VerticalAlignment="Center" Margin="30,20,30,20" HorizontalAlignment="Center">

                <!-- לוגו "ל" אדום כמו על רכבים בישראל -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text=" 🚗" FontSize="28" Height="41" Foreground="blue"/>
                    <TextBlock Text=" רישיון" FontSize="28" FontWeight="Bold" Foreground="#222222" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    <Border Width="50" Height="50" CornerRadius="8" Background="Blue">
                        <TextBlock Text="ל" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>

                <!-- Username -->
                <Label Content="שם משתמש" Foreground="{StaticResource SecondaryBrush}" FontWeight="Medium"/>
                <TextBox x:Name="UsernameTextBox" BorderThickness="1" BorderBrush="#CCC" Padding="8" FontSize="14" Margin="0,0,0,15"/>

                <!-- Password -->
                <Label Content="סיסמה" Foreground="{StaticResource SecondaryBrush}" FontWeight="Medium"/>
                <PasswordBox x:Name="PasswordInputBox" BorderThickness="1" BorderBrush="#CCC" Padding="8" FontSize="14" Margin="0,0,0,20"/>

                <!-- Login Button -->
                <Button x:Name="LoginButton" Content="התחבר" Style="{StaticResource ModernButton}" Click="LoginButton_Click" Margin="0,0,0,10"/>

                <!-- Error Message -->
                <TextBlock x:Name="ErrorMessageTextBlock" Foreground="Red" TextWrapping="Wrap" HorizontalAlignment="Center" MinHeight="30"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
