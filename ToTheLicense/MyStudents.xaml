<Window x:Class="ToTheLicense.MyStudents"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="התלמידים שלי" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F7F9FC" FlowDirection="RightToLeft" FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- כותרת -->
            <RowDefinition Height="Auto"/>
            <!-- חיפוש -->
            <RowDefinition Height="*"/>
            <!-- טבלה -->
            <RowDefinition Height="Auto"/>
            <!-- כפתור חזור -->
        </Grid.RowDefinitions>

        <!-- כותרת -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Border Background="#E3F2FD" CornerRadius="10" Width="50" Height="50" Margin="0,0,15,0">
                <TextBlock Text="🎓" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="רשימת התלמידים שלי" FontSize="24" FontWeight="Bold" Foreground="#002366"/>
                <TextBlock x:Name="SubtitleText" Text="סה''כ 0 תלמידים פעילים" FontSize="14" Foreground="Gray"/>
            </StackPanel>
        </StackPanel>

        <!-- שורת חיפוש -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="5" Margin="0,0,0,20" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="10,0"/>
                <TextBox x:Name="SearchBox" Grid.Column="1" BorderThickness="0" Background="Transparent" 
                         FontSize="14" Padding="5" VerticalContentAlignment="Center"
                         TextChanged="SearchBox_TextChanged"
                         Tag="חפש לפי שם תלמיד..."/>
            </Grid>
        </Border>

        <!-- טבלת נתונים -->
        <DataGrid x:Name="StudentsGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" Background="White" BorderThickness="0"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  RowHeight="40" FontSize="14">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="#444"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="שם פרטי" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="שם משפחה" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="טלפון / אימייל" Binding="{Binding Email}" Width="1.5*"/>
                <DataGridTextColumn Header="סוג רישיון" Binding="{Binding LicenseType}" Width="100"/>
                <DataGridTemplateColumn Header="שיעורים" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="#E8F5E9" CornerRadius="10" Padding="8,2" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding LessonsCount}" Foreground="#2E7D32" FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- כפתור חזור -->
        <Button Grid.Row="3" Content="חזור לדף הבית" HorizontalAlignment="Right" Margin="0,20,0,0"
                Background="Transparent" Foreground="#666" BorderThickness="1" BorderBrush="#CCC"
                Padding="20,8" Cursor="Hand" Click="BackButton_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>